class AppConstants {
  AppConstants._();
  static const String appName = "Organizer";
  static const Duration aniamtionDuration = Duration(milliseconds: 300);

  static const double borderRadius = 20;
}
